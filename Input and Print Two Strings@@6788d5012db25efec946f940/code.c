#include <stdio.h>

int main() {
    char a, b;  // Corrected the colon (:) to a comma (,)
    scanf("%c %c", &a, &b);

    printf("You entered: %c and %c", a, b);  // Added a comma between a and b
    return 0;
}
